/** Tonos de estado. Verde, rojo, ámbar y azul solo para estados operativos. */
export type Tone = "neutral" | "accent" | "success" | "danger" | "warning" | "info";

/** Temas: Oro Líquido (claro, por defecto) y Cava Reserva (oscuro). */
export type ThemeName = "oro" | "cava";
