// Fragmentos de clases compartidos por varios componentes.

/** Anillo de foco de 2 px con desplazamiento (05 §6). */
export const focusRing =
  "focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-focus";

/** Foco de campos de texto: borde en oro y anillo de 2 px pegado. */
export const fieldFocus =
  "focus:border-accent focus:outline-2 focus:outline-offset-1 focus:outline-focus";

/** Superficie de overlay: fondo del tema, radio lg y la única sombra del sistema. */
export const overlaySurface = "bg-bg text-fg shadow-overlay";

/** Etiqueta de tabla / sección en mayúsculas espaciadas. */
export const labelText = "font-ui text-2xs font-medium uppercase tracking-label text-fg-subtle";
